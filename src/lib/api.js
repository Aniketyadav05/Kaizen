/**
 * FinPilot - Google Sheets API Fetch Wrapper
 * Replaces the old Axios interceptor to communicate with Apps Script.
 */

import googleSheetsApi from "../services/googleSheetsApi";

export default googleSheetsApi;
